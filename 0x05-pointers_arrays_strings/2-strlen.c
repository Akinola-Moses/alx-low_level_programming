#include "main.h"

/**
 * _strlen - Return the lenght of a string.
 * @s: String to return the lenght.
 *
 * Return: The lenght.
 */

int _strlen(char *s)
{
	int l; /*Declare an it to loop through thestring*/

	for (l = 0; s[l] != '\0'; l++)
	{

	}
	return (l);
}
