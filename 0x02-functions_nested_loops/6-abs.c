#include "main.h"

/**
 * _abs - A function that compute the absolute value of an integer.
 * @p: Parameter.
 * Return: 0.
 */

int _abs(int p)
{

	if (p < 0)
	{
		return (-p);
	}
	else
	{
		return (p);
	}
}
